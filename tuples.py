#Tuples : structure similar to lists  where we can store multiple pieces of information
# is imutable, meaning it cannot change, Used for data that does not change

coordinates = (4, 5)

print(coordinates[1])


coordinates2 = [(4, 5), (6,7), (80, 34)]
print(coordinates2)
